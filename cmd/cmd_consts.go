package cmd

const skipValidationLatestTagOnTheHead = "skip-validation-latest-tag-on-the-head"
const skipValidationFirstVersion = "skip-validation-first-version"
const firstVersion = "0.1.0"

const repositoryLocation = "repository-location"
const versionedBranchNames = "versioned-branch-names"
const dryRun = "dry-run"
const tagPrefix = "tag-prefix"
const logLevel = "log-level"
const remoteName = "remote-name"

const pushTag = "push-tag"

const withPrefix = "with-prefix"
const withMetadata = "with-metadata"

const sortDirection = "sort-direction"
const maxListSize = "max-list-size"
const maxLogIteration = "max-log-iteration"
